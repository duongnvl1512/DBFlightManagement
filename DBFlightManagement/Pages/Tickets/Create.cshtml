@page
@model DBFlightManagement.Pages.Tickets.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>@ViewData["Title"]</h1>
<h4>Ticket</h4>
<hr />

<!-- Container for the form -->
<div class="row">
    <div class="col-md-4">
        <form method="post" id="create-ticket-form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <!-- FlightId -->
            <div class="form-floating mb-3">
                <input asp-for="Ticket.FlightId" class="form-control" placeholder="Flight ID" />
                <label asp-for="Ticket.FlightId" class="form-label"></label>
                <span asp-validation-for="Ticket.FlightId" class="text-danger"></span>
            </div>

            <!-- CustomerId -->
            <div class="form-floating mb-3">
                <input asp-for="Ticket.CustomerId" class="form-control" placeholder="Customer ID" />
                <label asp-for="Ticket.CustomerId" class="form-label"></label>
                <span asp-validation-for="Ticket.CustomerId" class="text-danger"></span>
            </div>

            <!-- BookingDate -->
            <div class="form-floating mb-3">
                <input asp-for="Ticket.BookingDate" class="form-control" placeholder="Booking Date" />
                <label asp-for="Ticket.BookingDate" class="form-label"></label>
                <span asp-validation-for="Ticket.BookingDate" class="text-danger"></span>
            </div>

            <!-- SeatBooked -->
            <div class="form-floating mb-3">
                <input asp-for="Ticket.SeatBooked" class="form-control" placeholder="Seat Booked" />
                <label asp-for="Ticket.SeatBooked" class="form-label"></label>
                <span asp-validation-for="Ticket.SeatBooked" class="text-danger"></span>
            </div>

            <!-- Sale -->
            <div class="form-floating mb-3">
                <input asp-for="Ticket.Sale" class="form-control" placeholder="Sale" />
                <label asp-for="Ticket.Sale" class="form-label"></label>
                <span asp-validation-for="Ticket.Sale" class="text-danger"></span>
            </div>

            <!-- TotalPrice -->
            <div class="form-floating mb-3">
                <input asp-for="Ticket.TotalPrice" class="form-control" placeholder="Total Price" />
                <label asp-for="Ticket.TotalPrice" class="form-label"></label>
                <span asp-validation-for="Ticket.TotalPrice" class="text-danger"></span>
            </div>

            <!-- Submit Button -->
            <div class="form-group mb-3">
                <button type="submit" class="w-100 btn btn-lg btn-primary">Create</button>
            </div>

            <!-- Back to List Button -->
            <div class="form-group">
                <a asp-page="Index" class="btn btn-secondary w-100 btn-lg">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<style>
    /* Create Ticket Page Container */
    .row {
        margin-top: 30px;
        display: flex;
        justify-content: center;
    }

    /* Create Ticket Form */
    #create-ticket-form {
        background-color: #ffffff;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.1);
    }

    /* Form Heading */
    h1, h4 {
        color: #333;
        font-weight: 600;
        text-align: center;
    }

    /* Form Fields */
    .form-floating {
        margin-bottom: 20px;
    }

        .form-floating input {
            border-radius: 8px;
            height: 45px;
            font-size: 1rem;
            padding: 15px;
            transition: border-color 0.3s ease;
        }

            .form-floating input:focus {
                border-color: #007bff;
                box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
            }

        /* Form Labels */
        .form-floating label {
            font-size: 1rem;
            color: #495057;
        }

    /* Submit Button Styling */
    button[type="submit"] {
        background-color: #007bff;
        color: white;
        border-radius: 25px;
        padding: 12px;
        font-weight: bold;
        border: none;
        transition: background-color 0.3s ease;
        width: 100%;
    }

        button[type="submit"]:hover {
            background-color: #0056b3;
        }

    /* Back to List Button Styling */
    .btn-secondary {
        background-color: #6c757d;
        color: white;
        border-radius: 25px;
        padding: 12px;
        font-weight: bold;
        border: none;
        transition: background-color 0.3s ease;
    }

        .btn-secondary:hover {
            background-color: #5a6268;
        }

    /* Validation error messages */
    .text-danger {
        color: #dc3545;
        font-size: 0.875rem;
    }

    /* Padding for the row and form to center */
    .col-md-4 {
        padding-left: 15px;
        padding-right: 15px;
    }

    /* Back to List Styling */
    a {
        font-size: 1rem;
        color: #ffffff;
        text-decoration: none;
    }

        a:hover {
            text-decoration: underline;
        }
</style>
