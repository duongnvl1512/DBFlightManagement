@page
@model DBFlightManagement.Pages.Tickets.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>

<style>
    .search-container {
        display: flex;
        align-items: center;
        gap: 10px;
        background-color: #f9f9f9;
        border: 1px solid #ccc;
        border-radius: 8px;
        padding: 10px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .search-input {
        flex: 1;
        border: none;
        outline: none;
        padding: 8px 12px;
        border-radius: 4px;
        font-size: 14px;
    }

        .search-input::placeholder {
            color: #888;
        }

    .search-btn {
        background-color: #007bff;
        border: none;
        color: white;
        padding: 8px 16px;
        border-radius: 4px;
        font-size: 14px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

        .search-btn:hover {
            background-color: #0056b3;
        }
</style>

<div class="search-container">
    <form method="get" style="width: 100%; display: flex; align-items: center;">
        <input type="text" name="SearchTerm" value="@Model.SearchTerm" class="search-input" placeholder="Search by Name, Address, Phone, or Email" />
        <button type="submit" class="search-btn">Search</button>
    </form>
</div>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ticket[0].FlightId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket[0].CustomerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket[0].DepartmentCity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket[0].ArrivalCity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket[0].BookingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket[0].SeatBooked)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket[0].Sale)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket[0].TotalPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Ticket) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FlightId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DepartmentCity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArrivalCity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SeatBooked)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sale)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalPrice)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TicketId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TicketId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TicketId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
